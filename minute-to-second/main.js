const convert = minute => minute * 60;

console.log('convert 5 minutes to seconds', convert(5))
console.log('convert 3 minutes to seconds', convert(3))
console.log('convert 2 minutes to seconds', convert(2))
